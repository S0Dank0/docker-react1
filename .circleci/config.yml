version: 2.1
jobs:
  build:
    docker:
      - image: ghcr.io/s0dank0/docker-react1:latest
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: |
            npm install
      - run:
          name: Run Tests
          command: |
            npm run test -- --coverage
